#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @n: input value
 * @m: input value
 * Return: return value
*/

int add(int n, int m)
{
	int sum = 0;

	sum = n + m;
	return (sum);
}
