#include <stdio.h>

/**
 * main - Entry point
 * Return: return 0 (Successfull)
*/

int main(void)
{
	int i;
	char x;

	for (i = 0 ; i <= 9 ; i++)
	{
		putchar(i + '0');
	}
	for (x = a ; x <= f ; x++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);

}
