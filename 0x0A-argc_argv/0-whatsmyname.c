#include <stdio.h>

/**
 * main - Entry point
 * @argc: input value
 * @argv: input value
 * Return: Always 0 (Success)
*/

int main(int __attribute__ ((unused)) argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
